name: fitness_tracker_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  http_parser: ^4.0.2
  shared_preferences: ^2.3.2
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  fluttertoast: ^8.2.8  # For toast notifications
  intl: ^0.19.0  # For date formatting
  formz: ^0.6.1
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Updated to latest

flutter:
  uses-material-design: true
  assets:
    - assets/images/